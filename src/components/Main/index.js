import React from 'react';
import LearnPage from '../LearnPage';
import NavBar from '../NavBar';

function Main(props) {
    return (
        <div>
            <NavBar/>
            <LearnPage/>
        </div>
    );
}

export default Main;